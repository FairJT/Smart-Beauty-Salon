# File 4 of 5 — Hardening & Ops

**This file is the best one for the free agent to do solo — all cards are 🟡 (agent drafts, human reviews).** Can run alongside Files 2–3.

## Rules (apply to every card)
1. Verify before building; if it exists, report and stop.
2. Never log tokens, passwords, or SMS/gateway keys.
3. Secrets come from config/env, never source.
4. No `Assert.True(true)`.

Flags: 🟢 safe · 🟡 agent drafts, human reviews · 🔴 STOP (human only).

---

### 4.1 — Secrets audit 🟡
**Steps:** Confirm no secrets are committed (only `.env.example` with placeholder values). List anything that looks like a real secret in tracked files.
**Done when:** report shows zero live secrets; flag any that need rotation. (A human rotates them.)

### 4.2 — Encrypt `NationalCode` at rest 🟡
**Files:** `ApplicationUser` + an EF value converter.
**Steps:** Add a value converter that encrypts `NationalCode` on write and decrypts on read, key from config/secrets.
**Done when:** the stored column is ciphertext; the value round-trips correctly in app code.
**Review focus:** key from secrets, not source.

### 4.3 — Real health checks 🟡
**Steps:** Replace the swagger-page curl health check with real endpoints: `/health/live` (process up) and `/health/ready` (checks DB connectivity). Update `docker-compose.yml` to use them.
**Done when:** readiness fails when the DB is down; compose uses the new endpoints.

### 4.4 — Structured logging 🟡
**Steps:** Add Serilog with a correlation id and `TenantId` enrichment, writing to a log sink. Make sure tokens/keys are never logged.
**Done when:** each request logs with correlation id + tenant; no secrets appear in logs.

### 4.5 — CI pipeline 🟡
**Files:** `.github/workflows/ci.yml` (new).
**Steps:** On push/PR: build SalonOS + SmartSalon + Flutter; run the tenancy tests + `flutter analyze`; fail the job on any error. Build images on tag (no deploy).
**Done when:** the workflow is green on a clean branch and red when a test is deliberately broken.
