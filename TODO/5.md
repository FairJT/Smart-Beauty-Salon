# File 5 of 5 — Flutter Consolidation & Wrap-Up

**Mostly agent-doable (🟡/🟢).** The last item is an epic that waits for Files 1–3.

## Rules (apply to every card)
1. Verify before building; if it exists, report and stop.
2. Verify-then-delete: never remove code without first proving it's unreferenced.
3. Client-side permission checks are **UX only** — the server still enforces everything.
4. No `Assert.True(true)`.

Flags: 🟢 safe · 🟡 agent drafts, human reviews · 🔴 STOP (human only).

---

### 5.1 — Consolidate to one HTTP client 🟡
**Why:** the app has both an `http`-based `ApiService` and a `dio_client`. Pick one.
**Steps:** Standardize on **Dio** with interceptors for Bearer-token injection and 401 → `session_expired`. Migrate all calls to it; remove the other client.
**Done when:** a single HTTP client; app builds and `flutter analyze` is clean.

### 5.2 — Permission-aware route guards 🟡
**Steps:** GoRouter guards currently gate by role. Gate by the JWT permission set instead (decoded client-side), so the UI matches the server's permission model. Still UX-only.
**Done when:** routes show/hide by permission; no screen relies on this for security.

### 5.3 — Remove dead / legacy Flutter code 🟡
**Steps:** Find any unreferenced legacy screens/providers (`grep` for imports; check `main.dart`). If unused, delete. If anything still imports it, stop and report instead of deleting.
**Done when:** dead code is gone; app still builds and analyzes clean.

### 5.4 — Decommission legacy SmartSalon 🔴 STOP (epic — not yet)
🔴 **STOP — human decision, and only later.** Do **not** remove SmartSalon until: File 2's parity matrix is complete, Flutter is pointed at SalonOS (2.4), and File 1's isolation tests pass. SmartSalon is the only backend currently serving real users.
**Agent may:** nothing yet. Revisit after Files 1–3 are done.
