# File 3 of 5 — Functional Gaps

**Do Files 1–2 first.** This file fills missing behaviour. Most of it is 🔴 STOP (money/auth/correctness). Only 3.1 is fully agent-doable.

## Rules (apply to every card)
1. Verify before building; if it exists, report and stop.
2. Read `payments/SKILL.md` (money) and `multi-tenancy/SKILL.md` first.
3. Tenant id only from the JWT claim.
4. 🔴 = STOP (human only); do only the "agent may" prep.
5. No `Assert.True(true)`.

Flags: 🟢 safe · 🟡 agent drafts, human reviews · 🔴 STOP (human only).

---

### 3.1 — Real notification / SMS sending 🟡 (agent-doable)
**Why:** `ReminderJob` and the notification handler only flag `ReminderSent` — nothing is actually sent.
**Steps:** Add a provider-agnostic sender behind an interface (`INotificationSender`), with an Iran SMS-gateway adapter and a push adapter, chosen by config. Wire it into `ReminderJob` and the notification handler.
**Done when:** a reminder is actually dispatched via the provider in a sandbox; the gateway is config-selected; no secrets in logs.

### 3.2 — Booking event handlers 🔴 STOP
🔴 **STOP — human/Claude writes the logic.** `BookingCompletedHandler` / `BookingCancelledHandler` must consume/restore inventory, update ratings, mark review-eligibility, notify — and be **idempotent** (the outbox retries).
**Agent may:** scaffold the empty handler classes + an idempotency-key check stub. Then stop.

### 3.3 — Payment provider + Zarinpal (deposits) 🔴 STOP
🔴 **STOP — human/Claude writes this.** Deposits have no payment rail. Needs provider-agnostic `IPaymentProvider` + Zarinpal adapter with idempotency keys and signature-verified webhooks.
**Agent may:** leave any existing interface skeleton untouched and report what exists. Then stop.

### 3.4 — Refresh-token system 🔴 STOP
🔴 **STOP — human/Claude writes this.** A 30-min token with no refresh logs users out constantly. Needs rotating, revocable, stored refresh tokens.
**Agent may:** report the current token-issuing code location. Then stop.

### 3.5 — Availability engine: two contract models 🔴 STOP
🔴 **STOP — human/Claude writes the rules.** Availability must branch on contract type — salon-set hours (salaried) vs artist-set calendar (chair/room rental) — block approved leave/holidays, and reserve slots without double-booking (concurrency).
**Agent may:** after the rules are authored, build the **read-only** "list available slots" view/endpoint. Not the reservation/locking logic.
