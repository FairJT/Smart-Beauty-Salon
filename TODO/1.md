# File 1 of 5 — Foundation: Reality Check & Tenant Isolation

**Do this file first. Finish it before File 2.**
Most cards here are 🔴 STOP — a human/Claude does them. The agent's job in this file is mostly to **inventory and report**, not to write isolation code.

## Rules (apply to every card, every file)
1. **Verify before building.** Check the real repo first. If the thing already exists, report and stop.
2. **Read** `multi-tenancy/SKILL.md` and `payments/SKILL.md` before touching tenant/money code.
3. **Tenant id only from the JWT claim** — never from body/query/route.
4. **🔴 = STOP.** Do not implement; hand to a human. Only do the "agent may" prep noted.
5. **No `Assert.True(true)`.** Tests must fail when behaviour is wrong.

Flags: 🟢 safe · 🟡 agent drafts, human reviews · 🔴 STOP (human only).

---

### 1.1 — Reconcile report vs actual repo 🟡
**Why:** the reports disagree on what exists. Find the truth before anyone builds.
**Steps:** Check whether these exist in the repo and note exists / partial / missing:
`src/SalonOS.Api/Program.cs`; the DbContext(s) and **where tenant query filters live**; RLS SQL scripts; the actual tests inside `tests/SalonOS.Tenancy.Tests/`; any payment provider.
**Done when:** a short report lists each item as exists/partial/missing, with file paths.

### 1.2 — Host `Program.cs` + middleware order 🔴 STOP
🔴 **STOP — human/Claude writes this.** The pipeline order (`UseAuthentication` → `TenantResolutionMiddleware` → `UseAuthorization`) is security-critical.
**Agent may:** report whether `Program.cs` exists and what middleware it currently registers. Then stop.

### 1.3 — Tenant query filter on EVERY module DbContext 🔴 STOP
🔴 **STOP — human/Claude writes this.** If modules use their own contexts (`IdentityDbContext`, `BookingDbContext`, …) but the filter lives in one `AppDbContext`, isolation has holes.
**Agent may:** list every DbContext in the repo and which entities each maps (so the human knows which contexts need the filter). Then stop.

### 1.4 — RLS scripts + session-context interceptor 🔴 STOP
🔴 **STOP — human/Claude writes this.** SQL predicate function + security policy on every tenant table, plus the `sp_set_session_context` interceptor.
**Agent may:** list every entity that inherits `TenantEntity` (these are the tables needing a policy). Then stop.

### 1.5 — Cross-tenant isolation tests 🔴 STOP
🔴 **STOP — human/Claude writes the assertions.** Tests: cross-tenant read/write blocked, IDOR blocked, privilege escalation blocked, platform-owner bypass works.
**Agent may:** build the two-tenant test fixture/seed data only (no assertions). Then stop.

### 1.6 — Outbox same-transaction guarantee 🔴 STOP
🔴 **STOP — human/Claude verifies/fixes.** The event row must be saved in the same transaction as the state change; separate DbContexts break this.
**Agent may:** trace which context writes `OutboxMessage` vs which writes the booking/state change, and report. Then stop.
