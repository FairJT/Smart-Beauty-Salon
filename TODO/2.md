# File 2 of 5 — Request Path: Serve SalonOS & Lock Controllers

**Do File 1 first.** This file gets the new backend into the actual request path. Mixed agent/human.

## Rules (apply to every card)
1. Verify before building; if it exists, report and stop.
2. Read `multi-tenancy/SKILL.md` and `payments/SKILL.md` first.
3. Tenant id only from the JWT claim.
4. 🔴 = STOP (human only); do only the "agent may" prep.
5. No `Assert.True(true)`.

Flags: 🟢 safe · 🟡 agent drafts, human reviews · 🔴 STOP (human only).

---

### 2.1 — Endpoint parity matrix (do this first) 🟡
**Why:** the Flutter app talks to the legacy backend (:5015). Before pointing it at SalonOS, know which endpoints exist on both.
**Steps:** List every endpoint the Flutter app calls. For each, mark whether SalonOS (:5016) has an equivalent. Flag every gap.
**Done when:** a matrix exists; all gaps are listed.

### 2.2 — Lock down controllers 🟡
**Steps:** Put `[HasPermission("...")]` on every SalonOS controller action, using the permission catalog (the 77 `permission` constants). Add `[Authorize]` or an explicit `[AllowAnonymous]` so **no action is ungated by omission**.
**Files:** all controllers under `src/Modules/*/.../Controllers/` and `src/SalonOS.Api/`.
**Done when:** every action has an explicit gate; produce an action→permission table; build passes.
**Review focus:** the permission on each action matches the role→permission map; nothing left open.

### 2.3 — DI wiring for authz/tenant/current-user 🔴 STOP
🔴 **STOP — human/Claude writes this.** Registering `IAuthorizationPolicyProvider`, `PermissionHandler`, `ICurrentUser`, `ITenantContext`, and module services wrong = silent authz bypass.
**Agent may:** report what is currently registered in `Program.cs`. Then stop.

### 2.4 — Point Flutter at `/api` (SalonOS) 🟡
**Depends on 2.1 (parity clean) and 2.3 (DI done).**
**Steps:** Change `ApiService.baseUrl` (and `dio_client`) to `/api` so Nginx proxies to SalonOS. Put it behind a config flag so you can switch back.
**Done when:** the app authenticates and loads data from SalonOS for endpoints that have parity; flag lets you revert.
